// This's an Abstract Chess plugin and must be put in the declarations folder

// The container of the timer plugin
chess.plugins.abstractChess.gameRecord = {
    neededPlugins: {
        // The timer plugin isn't included as only some functionalities need it
        // timer: chess.plugins.abstractChess.timer
        //
    },
    api: {},
    configuration: {},
    hook: {},
    unitTest: { hook: {} },
    compatibility: {}
};